node_modules/
dist/
dev-dist/
.wrangler/
.env
.env.*
!.env.remote-dev
*.log
*.tsbuildinfo
.DS_Store
.claude/settings.local.json
.claude/scheduled_tasks.lock
# Branch plans live in the PR description (collapsible details block), not the repo
.claude/plans/

# Playwright
test-results/
playwright-report/
.playwright-mcp/

# Agent tmp files
.tmp/
.threa-attachments/

# Dev-only persisted enclave EIK (never committed; prod uses ephemeral keys)
apps/enclave/.enclave-key.dev.json
